# 원본 SQLite 는 28MB 가 넘는다 — gzip 본(`*.db.gz`)만 추적하고, 실행 시 자동 해제해 쓴다
# (tools/mileage/db-util.mjs). 해제본을 커밋하지 않도록 여기서 막는다.
*.db
*.db-wal
*.db-shm

# 학기 갱신 직전의 DB 백업 — update-semester.mjs 가 build-db 전에 떠 둔다(신·구 백테스트
# 비교 기준이자 되돌릴 자리). 추적본은 mileage-history.db.gz 하나뿐이다.
mileage-history.prev.db.gz
